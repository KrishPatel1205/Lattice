{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-vault",
  "description": "Read and write only vault directories explicitly selected by the user through the native directory picker. The dialog grants the selected directory recursively; no global filesystem scope is granted.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "fs:allow-read-dir",
    "fs:allow-read-text-file",
    "fs:allow-write-text-file",
    "fs:allow-mkdir",
    "fs:allow-rename",
    "fs:allow-exists",
    "fs:allow-stat"
  ]
}
